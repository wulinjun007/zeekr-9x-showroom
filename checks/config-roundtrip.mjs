import assert from 'node:assert/strict';import {defaults,shareUrl,readSettings,text,locales} from '../app/experience.ts';
globalThis.window={location:{href:'https://example.test/',search:''}};
const original={...defaults,locale:'ar',section:'structure',view:'rear',paint:'forest',wheelStyle:'turbine',tireStyle:'touring',seatStyle:'cognac',backrest:'A',doors:['Door_LF','Hood'],hidden:['glass'],selected:'doors',isolated:true,explode:75,lights:true,radar:false,hotspots:false,transparent:true};
const url=shareUrl(original);window.location.search=new URL(url).search;assert.deepEqual(readSettings(),original);window.location.search='?view=unknown&explode=9999&ambient=javascript:x&doors=unknown';const safe=readSettings();assert.equal(safe.view,'hero');assert.equal(safe.explode,100);assert.deepEqual(safe.doors,[]);assert.equal(safe.ambient,defaults.ambient);for(const locale of Object.keys(locales))for(const key of ['safety','parkingAction','sourceText','wheelStyle','backrestNote','error'])assert.notEqual(text(locale,key),key);console.log('Configuration round-trip, invalid-input fallback and primary localization checks: PASS');
